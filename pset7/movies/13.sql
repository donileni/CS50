SELECT DISTINCT name FROM people JOIN stars ON stars.person_id = people.id where people.id IN (SELECT stars.person_id FROM stars WHERE stars.movie_id IN (SELECT stars.movie_id FROM stars WHERE stars.person_id = (SELECT people.id FROM people WHERE people.birth = 1958 AND people.name = "Kevin Bacon"))) AND people.name != "Kevin Bacon";